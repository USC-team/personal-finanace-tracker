package domain.useCase

/*
class TransactionUseCase(private val repo: TransactionRepository) {
    fun add(model: Transaction) = repo.add(model)
    fun update(model: Transaction) = repo.update(model)
}
*/